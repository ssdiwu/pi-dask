# test

Deterministic tests exercise the public contract, validation, wizard state transitions, and Pi adapter boundary. Tests do not require a live terminal; system UI calls are isolated at the extension boundary.
