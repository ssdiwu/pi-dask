# 选项嵌套在问题的有序数组中

问题使用 `id`、`title`、`description` 三个基础字段，并以有序的 `labels` 数组承载选项。每项选项使用 `id`、`label`、`value`、`description`；其完整路径为 `question.labels[].id` 等。选项 `id` 是调用方提供的用户可见序号，必须与数组顺序一致并从 `1` 连续递增；TUI 使用它展示选项，并让用户可在自填答案中按编号指代选项。
