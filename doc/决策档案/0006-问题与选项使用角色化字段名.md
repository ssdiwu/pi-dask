# 问题与选项使用角色化字段名

> Status: 已由 [0009](0009-选项嵌套在问题的有序数组中.md) 替代。

问题使用 `id`、`title`、`description` 三个基础字段；其枚举选项使用 `label_id`、`label`、`description` 三个基础字段。`description` 在两层都必填，分别解释问题和选项；角色差异通过 `title`/`label` 与其标识符字段名表达。
